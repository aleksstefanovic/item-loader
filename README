A simple nodejs app to produce random retail item objects, and output them in a file ready to be imported as JSON into a NoSQL database. 

HOW TO USE:
$1 is the amount of items you wish to add into the database
$2 is the amount of stores each item is available  at

node app.js $1 $2
